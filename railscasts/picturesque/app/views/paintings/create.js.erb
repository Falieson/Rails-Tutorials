<% if @painting.new_record? %>
	alert("Failed to upload painting: <%= j @painting.errors.full_message.join(', ').html_safe %>");
<% else %>
	$("#paintings").append("<%= j render(@painting) %>");
<% end %>